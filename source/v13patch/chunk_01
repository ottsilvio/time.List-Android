.label(UiFont.current(this)));
         fontButton.setOnClickListener(v -> chooseFont());
         box.addView(fontButton);
+        Button paletteButton = actionButton("课表配色：" + PaletteManager.label(PaletteManager.current(this)));
+        paletteButton.setOnClickListener(v -> choosePalette());
+        box.addView(paletteButton);
+        box.addView(palettePreview());
 
         box.addView(settingTitle("学期"));
         TextView semester = text(getSemesterStart() + " 起 · " + getSemesterWeeks() + " 周", 15, Color.rgb(46, 53, 66), false);
@@ -480,7 +485,7 @@ public class MainActivity extends Activity {
         box.addView(clear);
 
         box.addView(settingTitle("关于"));
-        box.addView(muted("1.2.0 · 针对长屏手机优化。数据仅保存在手机本地；图片识别使用设备端中文 OCR，导入后请核对周次、教室和考试时间。"));
+        box.addView(muted("1.3.0 · 莫兰迪 / 中国传统色双配色，液态玻璃课程卡片。数据仅保存在手机本地；图片识别使用设备端中文 OCR，导入后请核对周次、教室和考试时间。"));
 
         content.addView(scroll, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, 0, 1f));
         updateBottomTabs();
@@ -574,6 +579,39 @@ public class MainActivity extends Activity {
                 }).show();
     }
 
+    private void choosePalette() {
+        String[] labels = {"经典莫兰迪", "中国传统色"};
+        String[] values = {PaletteManager.MORANDI, PaletteManager.CHINESE};
+        String current = PaletteManager.current(this);
+        int checked = PaletteManager.CHINESE.equals(current) ? 1 : 0;
+        final int[] selected = {checked};
+        new AlertDialog.Builder(this)
+                .setTitle("课表配色")
+                .setSingleChoiceItems(labels, checked, (d, which) -> selected[0] = which)
+                .setMessage("切换后课程色卡和课表会同步更新。")
+                .setNegativeButton("取消", null)
+                .setPositiveButton("应用", (d, which) -> {
+                    prefs.edit().putString(PaletteManager.KEY, values[selected[0]]).apply();
+                    recreate();
+                }).show();
+    }
+
+    private View palettePreview() {
+        LinearLayout row = new LinearLayout(this);
+        row.setOrientation(LinearLayout.HORIZONTAL);
+        row.setGravity(Gravity.CENTER_VERTICAL);
+        row.setPadding(0, dp(5), 0, dp(3));
+        int[] colors = PaletteManager.colors(this);
+        for (int i = 0; i < colors.length; i++) {
+            View swatch = new View(this);
+            swatch.setBackground(roundRect(colors[i], 7));
+            LinearLayout.LayoutParams lp = new LinearLayout.LayoutParams(0, dp(18), 1f);
+            if (i > 0) lp.leftMargin = dp(3);
+            row.addView(swatch, lp);
+        }
+        return row;
+    }
+
     private void showAgendaNoteDialog(CourseBlock existing) {
         final boolean isNew = existing == null;
         String[] meta = isNew ? null : noteMeta(existing);
@@ -948,6 +986,7 @@ public class MainActivity extends Activity {
                     return;
                 }
                 db.appendAll(r.courses, r.exams);
+                db.mergeAdjacentExact();
                 Toast.makeText(this, "已追加导入", Toast.LENGTH_SHORT).show();
                 dialog.dismiss();
                 if (currentTab == 0) showSchedule(); else if (currentTab == 1) showAgenda();
@@ -958,6 +997,7 @@ public class MainActivity extends Activity {
                     return;
                 }
                 db.replaceWith(r.courses, r.exams);
+                db.mergeAdjacentExact();
                 Toast.makeText(this, "已覆盖导入", Toast.LENGTH_SHORT).show();
                 dialog.dismiss();
                 if (currentTab == 0) showSchedule(); else if (currentTab == 1) showAgenda();
@@ -997,15 +1037,16 @@ public class MainActivity extends Activity {
     private Button iconButton(String symbol) {
         Button b = new Button(this);
         b.setText(symbol);
-        b.setTextSize(23);
+        b.setTextSize("＋".equals(symbol) ? 23 : 16.5f);
         b.setAllCaps(false);
         b.setGravity(Gravity.CENTER);
         b.setPadding(0, 0, 0, 0);
-        b.setBackgroundColor(Color.TRANSPARENT);
-        b.setTextColor(Color.rgb(28, 34, 46));
+        b.setBackground(roundRect(Color.rgb(242, 237, 249), 16));
+        b.setTextColor(Color.rgb(96, 74, 148));
+        b.setElevation(dp(1));
         b.setMinWidth(0);
         b.setMinHeight(0);
-        UiFont.apply(this, b, false);
+        UiFont.apply(this, b, true);
         return b;
     }
 
diff --git a/app/src/main/java/com/timelist/app/PaletteManager.java b/app/src/main/java/com/timelist/app/PaletteManager.java
new file mode 100644
index 0000000..805fd13
--- /dev/null
+++ b/app/src/main/java/com/timelist/app/PaletteManager.java
@@ -0,0 +1,104 @@
+package com.timelist.app;
+
+import android.content.Context;
+import android.content.SharedPreferences;
+import android.graphics.Color;
+
+public final class PaletteManager {
+    public static final String KEY = "palette_style";
+    public static final String MORANDI = "morandi";
+    public static final String CHINESE = "chinese";
+    private static final String PREFS = "time_list_prefs";
+
+    // A calm, classic Morandi set. Slightly deepened so white text stays crisp.
+    private static final int[] MORANDI_COLORS = {
+            Color.rgb(156, 145, 181), // dusty violet
+            Color.rgb(122, 158, 190), // mist blue
+            Color.rgb(111, 161, 153), // sage teal
+            Color.rgb(183, 132, 145), // dusty rose
+            Color.rgb(132, 160, 126), // sage green
+            Color.rgb(190, 145, 105), // clay apricot
+            Color.rgb(109, 164, 176), // muted cyan
+            Color.rgb(164, 132, 158), // mauve
+            Color.rgb(175, 157, 104)  // antique gold
+    };
+
+    private static final String[] MORANDI_NAMES = {
+            "雾紫", "灰蓝", "鼠尾草青", "豆沙粉", "鼠尾草绿", "陶杏", "雾青", "藕灰紫", "古金"
+    };
+
+    // Chinese traditional-inspired palette: restrained mineral / plant tones.
+    private static final int[] CHINESE_COLORS = {
+            Color.rgb(105, 91, 134),  // 藕荷
+            Color.rgb(72, 126, 151),  // 黛蓝
+            Color.rgb(75, 145, 135),  // 竹青
+            Color.rgb(178, 90, 103),  // 胭脂
+            Color.rgb(105, 145, 91),  // 松绿
+            Color.rgb(197, 124, 75),  // 杏子
+            Color.rgb(71, 153, 166),  // 琉璃青
+            Color.rgb(143, 92, 128),  // 藤萝
+            Color.rgb(181, 151, 66)   // 松花
+    };
+
+    private static final String[] CHINESE_NAMES = {
+            "藕荷", "黛蓝", "竹青", "胭脂", "松绿", "杏子", "琉璃青", "藤萝", "松花"
+    };
+
+    private static final int[] LEGACY = {
+            SeedData.PURPLE, S