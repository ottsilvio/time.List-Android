diff --git a/app/build.gradle b/app/build.gradle
index 6cbc1b3..ca7ba09 100644
--- a/app/build.gradle
+++ b/app/build.gradle
@@ -11,7 +11,7 @@ android {
         minSdk 23
         targetSdk 35
         versionCode 4
-        versionName '1.3.0'
+        versionName '1.4.0'
     }
 
     buildFeatures {
diff --git a/app/src/main/java/com/timelist/app/CourseEditorDialog.java b/app/src/main/java/com/timelist/app/CourseEditorDialog.java
index 9ddffca..bba4cfd 100644
--- a/app/src/main/java/com/timelist/app/CourseEditorDialog.java
+++ b/app/src/main/java/com/timelist/app/CourseEditorDialog.java
@@ -67,22 +67,22 @@ public final class CourseEditorDialog {
         EditText weeks = new EditText(context);
         weeks.setText(isNew ? "1-20" : source.weeks);
         weeks.setHint("例如 1-8 或 1-4,7,9-12");
-        weeks.setTextSize(16);
+        weeks.setTextSize(16.5f);
         weeks.setSingleLine(true);
         weeks.setInputType(InputType.TYPE_CLASS_TEXT);
         styleField(context, weeks);
         root.addView(weeks, spacedLp(context, 4));
 
         TextView weekSummary = new TextView(context);
-        weekSummary.setTextSize(12.5f);
-        weekSummary.setTextColor(Color.rgb(111, 94, 153));
+        weekSummary.setTextSize(13.2f);
+        weekSummary.setTextColor(Color.rgb(103, 78, 163));
         weekSummary.setPadding(dp(context, 2), 0, 0, dp(context, 4));
         UiFont.apply(context, weekSummary, false);
         root.addView(weekSummary, lp());
 
         TextView weekHint = new TextView(context);
         weekHint.setText("1-8 表示第1至8周；逗号可设置不连续周次。删除课程时也会显示该课程的周次范围。");
-        weekHint.setTextSize(11.5f);
+        weekHint.setTextSize(12.2f);
         weekHint.setTextColor(Color.rgb(125, 127, 139));
         weekHint.setLineSpacing(0, 1.08f);
         UiFont.apply(context, weekHint, false);
@@ -172,7 +172,7 @@ public final class CourseEditorDialog {
         EditText e = new EditText(c);
         e.setHint(hint);
         e.setText(value == null ? "" : value);
-        e.setTextSize(16);
+        e.setTextSize(16.5f);
         e.setSingleLine(false);
         e.setMaxLines(3);
         e.setInputType(InputType.TYPE_CLASS_TEXT | InputType.TYPE_TEXT_FLAG_CAP_SENTENCES);
@@ -185,7 +185,7 @@ public final class CourseEditorDialog {
         e.setTextColor(Color.rgb(39, 39, 47));
         e.setHintTextColor(Color.rgb(153, 153, 163));
         e.setPadding(dp(c, 12), dp(c, 10), dp(c, 12), dp(c, 10));
-        e.setBackground(rounded(Color.rgb(249, 247, 252), dp(c, 12)));
+        e.setBackground(rounded(Color.rgb(247, 244, 251), dp(c, 12)));
         UiFont.apply(c, e, false);
     }
 
@@ -215,8 +215,8 @@ public final class CourseEditorDialog {
     private static void addLabel(Context c, LinearLayout parent, String text) {
         TextView t = new TextView(c);
         t.setText(text);
-        t.setTextSize(12.5f);
-        t.setTextColor(Color.rgb(98, 98, 111));
+        t.setTextSize(13.5f);
+        t.setTextColor(Color.rgb(103, 78, 163));
         t.setPadding(dp(c, 2), dp(c, 8), 0, dp(c, 5));
         UiFont.apply(c, t, true);
         parent.addView(t, lp());
@@ -273,7 +273,7 @@ public final class CourseEditorDialog {
         private View row(int position, boolean dropdown) {
             TextView t = new TextView(context);
             t.setText(items[position]);
-            t.setTextSize(15);
+            t.setTextSize(15.8f);
             t.setTextColor(Color.rgb(45, 45, 55));
             t.setGravity(Gravity.CENTER_VERTICAL);
             t.setPadding(dp(context, 10), dp(context, dropdown ? 11 : 8), dp(context, 8), dp(context, dropdown ? 11 : 8));
@@ -310,7 +310,7 @@ public final class CourseEditorDialog {
 
             TextView name = new TextView(context);
             name.setText(names[position]);
-            name.setTextSize(15);
+            name.setTextSize(15.8f);
             name.setTextColor(Color.rgb(43, 43, 52));
             UiFont.apply(context, name, false);
             row.addView(name, new LinearLayout.LayoutParams(0, ViewGroup.LayoutParams.WRAP_CONTENT, 1f));
diff --git a/app/src/main/java/com/timelist/app/MainActivity.java b/app/src/main/java/com/timelist/app/MainActivity.java
index 1b84a10..5fdf5fe 100644
--- a/app/src/main/java/com/timelist/app/MainActivity.java
+++ b/app/src/main/java/com/timelist/app/MainActivity.java
@@ -83,6 +83,7 @@ public class MainActivity extends Activity {
         if (!prefs.contains(KEY_START)) prefs.edit().putString(KEY_START, "2026-08-31").apply();
         if (!prefs.contains(KEY_WEEKS)) prefs.edit().putInt(KEY_WEEKS, 20).apply();
         if (!prefs.contains(KEY_SHOW_OFF)) prefs.edit().putBoolean(KEY_SHOW_OFF, false).apply();
+        if (!prefs.contains(UiFont.KEY)) prefs.edit().putString(UiFont.KEY, UiFont.SANS).apply();
         if (!prefs.contains(PaletteManager.KEY)) prefs.edit().putString(PaletteManager.KEY, PaletteManager.MORANDI).apply();
         if (db.countCourses() == 0) restoreSeed(false);
         applyUserScheduleRevision();
@@ -106,28 +107,30 @@ public class MainActivity extends Activity {
         topBar = new LinearLayout(this);
         topBar.setOrientation(LinearLayout.HORIZONTAL);
         topBar.setGravity(Gravity.CENTER_VERTICAL);
-        topBar.setPadding(dp(18), dp(6), dp(10), dp(3));
+        topBar.setPadding(dp(18), dp(6), dp(16), dp(3));
         topBar.setBackgroundColor(Color.rgb(249, 247, 252));
-        root.addView(topBar, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(66)));
+        root.addView(topBar, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(70)));
 
         titleBox = new LinearLayout(this);
         titleBox.setOrientation(LinearLayout.VERTICAL);
         titleBox.setGravity(Gravity.CENTER_VERTICAL);
         topBar.addView(titleBox, new LinearLayout.LayoutParams(0, ViewGroup.LayoutParams.MATCH_PARENT, 1f));
-        titleMain = text("", 23, Color.rgb(35, 34, 43), true);
-        titleSub = text("", 12.5f, Color.rgb(111, 108, 121), false);
+        titleMain = text("", 25, Color.rgb(35, 34, 43), true);
+        titleSub = text("", 14f, Color.rgb(111, 108, 121), false);
         titleBox.addView(titleMain);
         titleBox.addView(titleSub);
 
         addButton = iconButton("＋");
-        importButton = iconButton("识");
+        importButton = iconButton("▣");
         addButton.setContentDescription("添加课程");
         importButton.setContentDescription("从课表图片导入");
         addButton.setOnClickListener(v -> CourseEditorDialog.show(this, null,
                 Math.max(1, Math.min(7, weekdayToday())), 1, editorListener));
         importButton.setOnClickListener(v -> pickImage());
-        topBar.addView(addButton, new LinearLayout.LayoutParams(dp(48), dp(48)));
-        topBar.addView(importButton, new LinearLayout.LayoutParams(dp(48), dp(48)));
+        LinearLayout.LayoutParams addLp = new LinearLayout.LayoutParams(dp(40), dp(40));
+        addLp.rightMargin = dp(8);
+        topBar.addView(addButton, addLp);
+        topBar.addView(importButton, new LinearLayout.LayoutParams(dp(40), dp(40)));
 
         content = new LinearLayout(this);
         content.setOrientation(LinearLayout.VERTICAL);
@@ -138,7 +141,7 @@ public class MainActivity extends Activity {
         bottomBar.setGravity(Gravity.CENTER);
         bottomBar.setPadding(dp(10), dp(3), dp(10), dp(4));
         bottomBar.setBackgroundColor(Color.rgb(247, 244, 250));
-        root.addView(bottomBar, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(60)));
+        root.addView(bottomBar, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(68)));
         addBottomTab("▦\n课表", 0);
         addBottomTab("≡\n日程", 1);
         addBottomTab("○\n我的", 2);
@@ -150,7 +153,7 @@ public class MainActivity extends Activity {
     private void addBottomTab(String label, int